server:
  port: 8080 #the default port used for zuul service

zuul:
  ignoredServices: '*'
  routes:
    echo-service:
      path: /echo/**
      url: http://localhost:8091
      serviceId: echo_service_id
